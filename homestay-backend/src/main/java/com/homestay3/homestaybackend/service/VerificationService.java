package com.homestay3.homestaybackend.service;

/**
 * Service interface for handling verification related operations.
 */
public interface VerificationService {

    // TODO: Define methods for verification logic, e.g.,
    // Page<VerificationDto> getVerifications(Pageable pageable, String status, String username);
    // VerificationDto getVerificationDetails(Long userId);
    // void approveVerification(Long userId, String adminUsername);
    // void rejectVerification(Long userId, String adminUsername, String reason);

} 